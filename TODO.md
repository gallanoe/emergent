# TODO

## Web Mode

- [ ] Make workspace switcher accessible in web mode (currently only visible in Electron titlebar via `ContentTitlebar`)
- [ ] Add workspace management to settings page (add/remove buttons) so web-only users can manage workspaces without direct RPC calls
